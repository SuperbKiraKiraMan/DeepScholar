# 安全策略 / Security Policy

## 数据与密钥边界

- 真实 API Key、Token、私钥和密码只能通过本地环境变量提供。
- .env、mcp_servers.json、SQLite 数据库、Zotero PDF、向量索引、模型缓存、记忆文件、transcript 和评测原始数据不属于公开仓库内容。
- 运行日志、SSE payload 和错误信息不得包含密钥或完整论文正文。
- 启用 MCP 时只允许显式配置的 server 和 tool，并继续执行本地 schema 与安全策略。

## 报告安全问题

请不要在公开 Issue 中粘贴密钥、个人数据或完整日志。如果仓库已启用 GitHub 的 Private vulnerability reporting，请使用该私有渠道；否则请先联系仓库维护者建立私密报告渠道，再提供最小化复现信息。

报告中建议包含：

1. 受影响的文件、版本或提交；
2. 复现步骤和影响范围；
3. 已脱敏的日志或最小 fixture；
4. 你建议的修复方向（如有）。

如果密钥可能已经泄露，请先立即在对应服务商处撤销并轮换，再进行报告。不要把真实密钥写入 Issue、PR、测试 fixture 或文档。

## 支持范围

项目维护者会优先处理可在当前公开代码中复现的安全问题。第三方 API、模型、数据源和 Docker 基础镜像的漏洞应同时向其官方安全渠道报告。
